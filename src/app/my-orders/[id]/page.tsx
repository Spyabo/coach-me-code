import { getUserByClerkId } from "@lib/mongo/users";
import { getListings } from "@lib/mongo/listings";


export default async function MyOrders({ params }) {
    // const { listings }: getListingsResponse = await getUserByClerkId(params.id)
    return (

       
    )
}